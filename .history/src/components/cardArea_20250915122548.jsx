
export default function cardArea({ cards }) {
    return (
        <>
            {cards && Array.isArray(cards) && cards.map((card) => {
                    <section st className="w-[100%] h-auto p-2 ">

                    </section>
                })
            }
        </>
    )
}